# OTA 与相控阵增量升级计划

依据 `OTA升级依据.md` 与用户提供的 `uav_pattern/farfield_resolution1deg.txt`。从4gps提交ccf329f继续，保留500MS/s→8SPC→FIR/D8→62.5MS/s、A/B共核及真值/观测/估计隔离。中文注释、UTF-8/LF、120列。原112项回归不能删除。

## 本次实现

1. 导入真实远场文本，保存H/V复幅相、角网格、源散列及数据资格；支持完整双端口输入但不得用一组激励伪造四个Jones元素。接入RP1/RP2主链并提供真实数据示例。
2. 统一列向量坐标约定，完整ZYX姿态/四元数、安装旋转、杆臂和相位中心进入天线算子；观测与真值分别计算姿态。
3. 引入实际阵元坐标、Tx权重/通道误差、阵元信号、Rx DBF、扫描、极化与独立校准求解，并连接正式链路；报告波束指向/HPBW/SLL及功率归一约定。
4. 提供有状态高阶分数延迟及1/2/5/10/20MHz误差比较；增加由独立频域测量拟合的频率相关校准和相关误差预算示例，明确合成输入资格。
5. 集成测试、静止退化、数据文件回归、报告与演示；按当前4gps工作流提交并上传。

## 数据边界

文件有一组Horizontal/Vertical的幅相。用户已确认2.8GHz、H/V同时激励；相对幅相、基定义及相位参考仍待确认。已完成解析、双独立激励接口及合成验证。真实数据示例采用显式未确认的Ludwig-3约定，仅验证联合模式，不能升级为完整双偏振资格。实测VNA、误差协方差、阵元嵌入式方向图、RTL和板卡文件未提供，不补造测量或宣称数字孪生完成。

## 并行边界

- 天线/姿态任务只修改pattern、指定airborne和新几何工具及自身测试。
- 阵列任务只新增radar_array、独立示例和自身测试。
- 宽带任务新增calibration/replay/verification模块，并在接口约定后负责共享核心宽带集成和自身测试。
- 主代理负责配置、物理主链集成、边界修复、文档、输出、全套验收和发布；完成的独立任务交叉审查集成。
