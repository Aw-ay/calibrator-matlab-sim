# 交付检查记录

验证环境：Windows，MATLAB R2025a。最终运行日志为 `../results/acceptance/final_run.log`。

## 交付范围

- 原设计 124 个函数名称中，118 个基线接口已有源码；6 个待确认扩展保持关闭。
- 共 136 个 MATLAB 文件，包括程序入口、算法模块、测试和检查工具。
- 注释、使用说明、图表标题与轴标签采用中文；接口名、单位和状态码保留约定英文。
- A/B 使用同一仪器核心；新增的独立模块明确其基线范围，不声称所有子模型均已接入主链。

## 最终验证

| 项目 | 实际结果 |
|---|---|
| 自动测试 | 93 通过，0 失败，0 未完成 |
| MATLAB Code Analyzer | 136 个文件，0 条诊断 |
| 固定平台 A，目标 20000 m | 估计 19999.999835 m |
| 飞行平台 B，目标 20000 m | 估计 19999.799212 m |
| A/B 静止退化 | 波形及 PDW 一致 |
| DMA 停顿及 bank 耗尽 | 回放时刻不漂移，耗尽显式丢失，不覆写忙 bank |
| 半双工 DDS/AWG | 完整发射区间与接收保护窗统一检查 |
| 导航真值隔离 | 独立有偏观测保留相应距离误差；未来观测不能启用发射 |
| 清单导出 | 复矩阵递归保存为实部、虚部、形状，可还原 |

A 输出目录：`../results/A_20260906_192845_354`。

B 输出目录：`../results/B_20260906_192859_646`。

已目视检查 A 演示图：中文显示正常，四联图无裁切，雷达距离峰位于 20 km。

以上是指定假设模型的数值验证结果，不是实测精度指标。原设计完整 T01–T40 资格另见 `original_spec_matrix.csv`，未覆盖的硬件/计量场景没有被提升为 PASS。

## 已记录的工程选择

沿用现有设计，交付基础 MATLAB 复包络工程；不依赖额外信号处理工具箱。宽带高保真、逐拍 RTL、实测复方向图、全校准及六项未确认扩展保持明确边界。B 主链使用短块冻结真实几何与恒速度观测预测；完整连续运动另有独立迟滞几何工具供后续接入。这些选择的代价是不能用本次结果直接签发板卡或整机资格。

开发过程中的失败测试与首轮失败导出记录保留在 `results/development_logs` 或对应未完成输出目录；分发压缩包仅包含最终有效演示和验收结果。
